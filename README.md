# This is the boot camp for DevOps For Flask Application

To run the flask application

Install Python and Activate Virtual Environment for Phyton

    python3 -m venv venv
    . venv/bin/activate
    pip install Flask

To Run the Application, enter

    export FLASK_APP=hello
    flask run
